Stage2 plan (internal)

1) routes/web.php: add GET /dashboard/data with same middleware group as dashboard main
2) DashboardController: add indexData(Request $request) -> return response()->json($service->getDashboardDataForUser())
3) DashboardService: add getDashboardDataForUser(User $user) and role-aware aggregations:
   - apply branch filter (for admin_toko/sales) using $user->branch_id
   - sales filter by created_by = auth()->id() and/or created_by column
   - compute required stats using aggregate queries:
     * rentals counts by status
     * rentals today/week/month via whereDate/whereBetween
     * revenue today/month via payments->sum(amount) with join/rental_date filter through whereHas or joins
     * invoice today / receipt today / returns today via rentals/payments/returnRecord (rental_returns)
   - graphs:
     * rental 30 days: single aggregate groupBy date
     * omzet 30 days: aggregate groupBy paid_at date
     * status rental: aggregate count by rental_status
     * top products: aggregate sum(quantity) grouped by product
   - widgets:
     * latest activity: ActivityLog recent (aggregate + eager)
     * latest transactions: rentals latest with customer/items via eager loading
     * latest invoices: rentals latest select invoice_number + total_amount + payment_status
     * reminders based on due dates and payment_status
     * products almost empty: products with stock_available <= threshold from config/settings if exists else DB field only
     * newest customers: customers latest
     * top products (reuse)
4) Blade: upgrade 3 templates to render skeleton and fill cards/charts/widgets from polling endpoint using fetch('/dashboard/data') every 10s.
5) Run: php artisan optimize:clear, route:list, test.

